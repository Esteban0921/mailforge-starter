generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ============================================================
// Este schema se irá ampliando fase a fase.
// Empieza vacío a propósito. La Fase 1 lo rellenará.
// ============================================================

// model User {
//   id           String   @id @default(cuid())
//   email        String   @unique
//   passwordHash String
//   name         String?
//   createdAt    DateTime @default(now())
//   updatedAt    DateTime @updatedAt
// }
